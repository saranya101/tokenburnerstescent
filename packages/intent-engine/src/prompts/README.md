# Prompt assets

Prompts may produce only `IntentDraftV1` candidates. Their untrusted output must pass the canonical strict Zod schema. They must never describe operation sequences or access bank-write clients.
